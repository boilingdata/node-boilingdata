import { IBoilingData, BoilingData, IBDQuery, IBDCallbacks } from "./boilingdata/boilingdata";
const _WebSocket = WebSocket;

export { IBoilingData, BoilingData, IBDQuery, _WebSocket as WebSocket, IBDCallbacks };
